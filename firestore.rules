rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // COMPLETELY OPEN FOR TESTING - REMOVE IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
